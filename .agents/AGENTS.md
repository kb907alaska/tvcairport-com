# Workspace Rules for TVC Cherry Capital Airport Project

- **Media Policy**: Strictly enforce zero AI images and zero generic stock photos. Only use authentic first-party TVC assets stored in `public/media/` and `public/brand/`.
- **Operational Data Disclaimer**: Always display `AIRPORT PROTOTYPE · SAMPLE FLIGHT DISPLAY · NOT LIVE OPERATIONAL DATA` on flight status displays.
- **Build Verification**: Run `npx astro build` after every major route edit or media update to ensure clean static generation.
